import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pull-to-refresh',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
